name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - run: npm run build
        env:
          GITHUB_PAGES: "true"
          # Repository variable CLOUD_API (Settings → Secrets and variables →
          # Actions → Variables). Unset = empty = cloud engine stays hidden and
          # the site builds exactly as it does today. Set it to the API's
          # public origin to light up cloud clipping, accounts, and billing.
          NEXT_PUBLIC_CLOUD_API: ${{ vars.CLOUD_API }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
